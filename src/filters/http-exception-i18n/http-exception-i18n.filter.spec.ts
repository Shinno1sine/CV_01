import { HttpExceptionI18nFilter } from './http-exception-i18n.filter';

describe('HttpExceptionI18nFilter', () => {
  it('should be defined', () => {
    expect(new HttpExceptionI18nFilter()).toBeDefined();
  });
});
